@inherits BlazorKitComponentBase

<DynamicElement Attributes="@UnknownParameters" TagName="div" class="@classname">
    @ChildContent
</DynamicElement>

    @functions {
      private string classname
      {
        get
        {
          var color = Color != InverseColor.None ? Color.GetValue<InverseColor>("uk-") : null;
          return $"{color} {Class}";
        }
      }

      [Parameter] private InverseColor Color { get; set; } = InverseColor.None;
      [Parameter] private string Class { get; set; }
      [Parameter] private RenderFragment ChildContent { get; set; }
    }
