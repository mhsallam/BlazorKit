@inherits BlazorKitComponentBase

<DynamicElement TagName="ul" class="@classname" Attributes="@UnknownParameters">
  @ChildContent
</DynamicElement>

@functions {
  private string classname
  {
    get
    {
      var isVertical = IsVertical ? "uk-thumbnav-vertical" : null;
      return $"uk-thumbnav {isVertical} {Class}";
    }
  }
  [Parameter] private string Class { get; set; }
  [Parameter] private bool IsVertical { get; set; }
  [Parameter] private RenderFragment ChildContent { get; set; }
}